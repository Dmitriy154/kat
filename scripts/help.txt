1. выгрузка из БД в JSON :
выполняется вызовом pbz.by/db/export.php, результат gm.json (# Последовательности из ≥2 пустых строк в середине — заменяются на @N, где N — количество пустых), который необходимо раскодировать, 
для этого применяется функция decompressSubstance(compressed);